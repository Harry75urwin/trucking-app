{
  "name": "trucking-app",
  "version": "1.0.0",
  "description": "Full-stack trucking platform with a Vite + React frontend and NestJS backend",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "dev:web": "pnpm --filter web dev",
    "dev:backend": "pnpm --filter backend start:dev",
    "build": "pnpm -r build",
    "build:web": "pnpm --filter web build",
    "build:backend": "pnpm --filter backend build",
    "start": "pnpm --filter backend start:prod",
    "lint:web": "pnpm --filter web exec eslint . --ext .ts,.tsx",
    "lint:backend": "pnpm --filter backend lint",
    "lint": "pnpm lint:web && pnpm lint:backend",
    "format": "pnpm --filter backend format && pnpm --filter web exec prettier --write .",
    "format:check": "pnpm --filter backend format:check && pnpm --filter web exec prettier --check .",
    "test": "pnpm --filter backend test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}